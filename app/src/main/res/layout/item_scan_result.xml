<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="item"
            type="cz.covid19cz.app.ui.sandbox.entity.ScanResultEntity" />

        <variable
            name="vm"
            type="cz.covid19cz.app.ui.sandbox.SandboxVM" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="@dimen/card_radius"
        app:contentPadding="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ProgressBar
            android:id="@+id/progress"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/Widget.AppCompat.ProgressBar.Horizontal"
            android:max="100"
            android:progress="@{100 + item.currRssi}"
            app:layout_constraintTop_toTopOf="parent"/>

        <TextView
            android:id="@+id/textDeviceId"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/progress"
            android:textColor="@color/textColorSecondary"
            android:textSize="24sp"
            android:text="@{item.deviceId}"/>

        <TextView
            android:id="@+id/textLastUpdate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/textDeviceId"
            android:text='@{String.format("Čas expozice: %s", item.sessionDurationString)}'/>

        <TextView
            android:id="@+id/textCurrentRssi"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/progress"
            android:textSize="20sp"
            android:textColor="@{item.inRange ? @color/textColorSecondary : @color/red}"
            android:text='@{item.inRange ? String.format("RSSI: %d", item.currRssi) : "Mimo dosah"}' />

        <TextView
            android:id="@+id/textMaxRssi"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/textCurrentRssi"
            android:text='@{String.format("Maximum: %d", item.maxRssi)}'/>

        <TextView
            android:id="@+id/textAvgRssi"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/textMaxRssi"
            android:text='@{String.format("Průměr: %d", item.avgRssi)}'/>

        <TextView
            android:id="@+id/textMedRssi"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/textAvgRssi"
            android:text='@{String.format("Medián: %d", item.medRssi)}'/>

    </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>
</layout>